[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "axjab-pykit"
version = "0"
description = "Shared Python utilities for axjab services"
requires-python = ">=3.13"
dependencies = [
    "requests"
]

[tool.setuptools.packages.find]
where = ["."]
